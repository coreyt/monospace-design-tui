# 0.3.0 Handoff B

Use this prompt to hand off the next `0.3.0` implementation pass to another
agent.

---

You are continuing `0.3.0` work for **Monospace Design TUI** in the repository
root.

Your job is to move the new **designer** capability from planning/spec/examples
toward an implementable shape, while staying strictly within the `0.3.0`
boundary.

## Context

Mono now has a `0.3.0` design package for the new **designer** system.

Read these first:

1. `dev/0.3.0-scope.md`
2. `dev/ARCHITECTURE-designer.md`
3. `dev/designer/README.md`
4. `dev/designer/yaml-spec.md`
5. `dev/designer/dsl-spec.md`
6. `dev/designer/dsl-to-ascii-projector.md`
7. `dev/designer/component-designs.md`
8. `dev/designer/examples/README.md`

Also use these as source-of-truth design references:

1. `monospace-tui-design-standard.md`
2. `monospace-tui-pattern-library.md`
3. `TUI-DESIGN.template.md`

## Current State

The repo already contains:

- `0.3.0` scope and roadmap docs
- the designer architecture
- designer DSL/YAML/projection specs
- high-level requirements for the future design agent, skill, and tools
- example artifacts for:
  - `2` navigation examples
  - `3` workflow examples
  - `15` screen examples

The examples live under:

- `dev/designer/examples/`

The current model is:

- YAML is canonical
- DSL is minimal and normalized
- ASCII is the primary review output
- ASCII is projected from canonical artifacts

## 0.3.0 Constraints

Stay inside `0.3.0`.

That means:

- Python-first is correct
- YAML remains canonical
- ASCII remains the primary human-facing output
- the DSL stays minimal
- do not invent a large future DSL
- do not jump to full runtime/framework code generation
- do not build deep codebase inference or artifact-graph infrastructure

This pass must remain **Mono-native**, not a generic wireframing system.

Default behavior should align with:

- the Monospace Design TUI standard
- the pattern library
- project overrides from `TUI-DESIGN.md`

Only allow drift when explicitly required by the user/project.

## What To Do

Review the existing `dev/designer/` package and identify the next concrete
implementation step that most increases practical value for `0.3.0`.

The likely direction is one of these:

1. define the Python package/module layout for the designer toolchain
2. define concrete schema/dataclass models for navigation/workflow/screen
   artifacts and normalized DSL objects
3. define the CLI or tool contracts for:
   - generate navigation
   - generate workflow
   - generate screen
   - revise artifact
   - project ASCII
4. define how a dedicated Mono designer agent/skill should invoke those tools
   predictably

Pick the best next step based on the repo state.

Then implement the documentation/spec work for that step.

## Output Quality Bar

The result should be:

- clear
- concrete
- implementable
- consistent with the current docs
- useful to a future coding agent that will build the actual Python tools

Avoid vague strategy language.
Prefer contracts, interfaces, module boundaries, schema fields, and explicit
responsibilities.

## Deliverable Style

If you create docs, they should:

- live under `dev/designer/` unless there is a strong reason otherwise
- be tightly scoped
- use the repo’s existing terminology
- distinguish between:
  - in scope for `0.3.0`
  - deferred to later releases

If you update existing docs, ensure consistency with:

- `dev/0.3.0-scope.md`
- `dev/ARCHITECTURE-designer.md`
- `dev/0.4-0.8-roadmap-design-agent.md`

## Success Criteria

Your pass is successful if:

- the next implementation step for the designer system is clearly defined
- the new material is concrete enough to code from
- the work remains aligned with Mono’s standard and pattern vocabulary
- the `0.3.0` boundary is preserved
- the resulting docs reduce ambiguity for the future tool/agent build

## Working Guidance

- Start by reading, not assuming.
- Reuse the existing example artifacts.
- Prefer stable naming and explicit contracts.
- Keep one coherent mental model across navigation, workflow, and screen
  artifacts.
- Treat ASCII as a projection, not the source of truth.
- Treat the designer agent as an orchestrator over Mono-authored tools, not as
  a free-form generator.

## Optional Strong Next Step

If the repo state supports it, a strong next step would be:

- `dev/designer/python-package-layout.md`
- `dev/designer/schema-models.md`
- `dev/designer/tool-contracts.md`
- `dev/designer/agent-skill-invocation.md`

Those would make the jump from architecture/specs to actual implementation much
cleaner.

---

Do not restart the design. Continue it.
Treat the existing `dev/designer/` package as the baseline and tighten it into
an implementation-ready shape.
